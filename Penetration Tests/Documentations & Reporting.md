## tmux
```
	# Start a new session
	tmux new -s <sessionname>

	# Split screen horizontally:
		Ctrl +B
		Shift + "

	# Split screen vertically:
		Ctrl + B
		Shift + %

	# Start loggiing:
		Ctrl + B
		Shift + P
		Shift + P again to stop
```